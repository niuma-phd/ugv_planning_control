# 科目二旧版上游接入手册（已归档）

本文件记录的 guard、recovery、`trusted_odom`、`odom_valid` 与 `navigation_enabled` 控制链已经退役，旧命令不得用于当前版本。

当前生产链为：

```text
/livox_odometry_mapped
  -> lio_odom_adapter
  -> /localization/odom
  -> waypoint_controller_node
  -> /cmd_vel
```

请只使用以下当前文档：

- [科目二顺序航点控制与现场调参说明](科目二_自主导航使用说明.md)
- [RDK 部署说明](../RDK_DEPLOYMENT.md)
- [实车接口与待办](实车接口与待办.md)

旧版手册正文已删除，避免误复制已经不存在的 launch 参数、话题和 fixture 模式。历史实现仍可通过 Git 记录查看。
