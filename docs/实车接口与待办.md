# 科目二实车接口与待办

本文只记录科目二上车前必须闭合的跨团队接口、实测参数和安全门禁。代码逻辑、构建与启动见[科目二自主导航使用说明](科目二_自主导航使用说明.md)，路径数据契约和测试步骤见[科目二上游接入与测试手册](科目二_上游接入与测试手册.md)。

## 1. 系统边界

当前车辆为两轮差速履带车，最终接口固定为：

```text
topic: /cmd_vel
type:  geometry_msgs/msg/Twist

linear.x   前进速度，m/s，正值向前
angular.z  偏航角速度，rad/s，正值左转
其他分量   必须为 0
```

本仓库不输出履带转速、PWM、油门、转角或 CAN 帧。左右履带换算、符号适配、硬件急停和独立命令超时看门狗由底盘团队负责。`Twist` 本身没有时间戳，因此底盘不能把“ROS 发布节点还活着”当作唯一安全条件。

主要接口：

| 方向 | 话题 | 类型 | 责任 |
|---|---|---|---|
| 输入 | `/livox/lidar` | `sensor_msgs/msg/PointCloud2` | Horizon 驱动发布；LIO 使用 `msg_type=1` |
| 输入 | `/livox/imu` | `sensor_msgs/msg/Imu` | Horizon 驱动发布；量纲和重力缩放必须实测 |
| 输入 | `/livox_odometry_mapped` | `nav_msgs/msg/Odometry` | 外部 LIO 的 `world→livox_frame` 位姿 |
| 上游标准输入 | `/subject2/path` | `nav_msgs/msg/Path` | 本团队适配器发布；合作方不必直接实现 ROS |
| 内部 | `/localization/odom` | `nav_msgs/msg/Odometry` | 规范 `odom→base_link` |
| 内部 | `/localization/trusted_odom` | `nav_msgs/msg/Odometry` | 通过保护后的控制输入 |
| 内部 | `/localization/odom_valid` | `std_msgs/msg/Bool` | 定位有效门禁 |
| 输出 | `/cmd_vel` | `geometry_msgs/msg/Twist` | 唯一最终车体速度命令 |

运行图中 `/cmd_vel` 必须恰好只有一个发布者。

## 2. TF 与外参责任

目标 TF 链：

```text
map → odom → base_link → livox_frame
```

- `base_link→livox_frame`：本团队测量并发布静态 TF。
- `odom→base_link`：本仓库根据 LIO 雷达位姿和已审核雷达外参计算。
- `map→odom`：当前临时发布恒等变换，只适用于物理起点、路径起点和轴向已经人工对齐的封闭场地。
- LIO 原始 TF 被隔离到 `/lio_raw/tf`、`/lio_raw/tf_static`，不能与规范 TF 树争夺发布权。
- 其他传感器静态 TF 由对应团队测量；本团队提供坐标、精度和记录要求。

### 雷达静态外参测量记录

每次测量必须形成可追溯记录：

1. 明确 `base_link` 和 `livox_frame` 的物理原点与轴向。
2. 测量平移 `x/y/z`，单位 m；测量固定轴姿态 `roll/pitch/yaw`，单位 rad。
3. 记录工具、方法、日期、车辆载荷、安装状态和各轴不确定度。
4. 生成非空记录编号 `lidar_extrinsics_provenance`，由另一人复核。
5. 静止采集点云，验证地面高度、车头方向和已知目标物位置。
6. Horizon 与 Avia 若安装在同一机械位置，仍需分别记录传感器型号和实际安装复核结果，不能只复制数字。

只有同时提供六个有限数值、非空记录编号并设置 `publish_lidar_static_tf:=true`，规范里程计和静态 TF 才会启用。默认零值不是有效外参。

## 3. 车辆参数回填

下列值必须来自实测或底盘团队批准值：

```text
车辆最大长度（含固定突出物）：______ m
车辆最大宽度（含履带）：______ m
车辆最大高度：______ m
最低离地间隙：______ m
是否可采用矩形 footprint：______
如否，base_link 下逆时针顶点：________________

base_link 物理位置：________________________________
base_link 相对车头、左缘、地面的尺寸：______________
规划/控制参考点相对 base_link 的 x/y/z：____________

最大允许前进速度：______ m/s
首次联调安全速度：______ m/s
最大稳定偏航角速度：______ rad/s
最大曲率：______ 1/m
最小转弯半径：______ m
最大线加速度：______ m/s²
最大减速度：______ m/s²
最大偏航角加速度：______ rad/s²
命令归零至开始减速的 95/99 分位延迟：______ / ______ s
指定低速停车距离：速度______ m/s，距离______ m
允许倒车：是 / 否
允许原地转向：是 / 否
```

建议测量方法：

- 速度/角速度：在低附着和高附着地面分别重复直线、定半径转弯，记录稳定段 p95。
- 加减速度：使用独立时间基准记录 `/cmd_vel`、底盘反馈和外部位置，计算斜率。
- 命令延迟：反复从稳定非零命令切换到零，统计底盘开始减速的 p95/p99。
- 停车距离：从首次联调速度开始，至少重复 10 次急停和正常归零，使用最差值而非均值。
- 履带符号：架空履带后分别发送小正值 `linear.x`、`angular.z`，确认前进和左转方向。
- 定位保护阈值：从健康 Horizon/LIO bag 统计周期、延迟、单帧平移和偏航增量，再设置超时与跳变阈值。

当前 YAML 中的速度、曲率、超时和跳变阈值均为候选值，不是实车认证结果。

## 4. 上车前门禁

### 4.1 雷达、LIO 与 TF

- 驱动、LIO 和本仓库固定到已审核提交。
- 使用验证过的 Horizon broadcast-code 白名单，生产运行禁止依赖自动发现。
- `/livox/lidar` 确认为 PointCloud2，LIO `msg_type=1`。
- `base_link→livox_frame` 与测量记录完全一致。
- `map→odom→base_link→livox_frame` 每一条边只有一个发布权威。
- IMU 单位、重力缩放和时间同步已经用静止与运动数据确认。

### 4.2 上游路径

- 合作方原始数据格式、传输方式、坐标原点、轴向、单位、点序、刷新和异常表达已确认。
- 独立适配器已经把原始数据转换为合格的 `/subject2/path`。
- 路径坐标原点和方向满足当前恒等 `map→odom` 临时假设。
- 用仓库 fixture 复核路径断流、非法/重复时间戳、错误 frame、空路径和 NaN/Inf 均归零；QoS 不匹配需在真实适配器联调时另行验证。

### 4.3 底盘与安全

- 下游已确认 `linear.x>0` 前进、`angular.z>0` 左转。
- 下游独立看门狗、现场急停和遥控停车均有效。
- 车辆速度、角速度、曲率和停车距离已经回填并批准。
- 执行器断开测试全部通过后，才允许架空履带；架空测试通过后，才允许封闭场地低速测试。

## 5. 当前未实现与待办

### P0：形成基础闭环

1. 确认合作方真实上游数据，开发独立适配器。
2. 完成 Horizon 外参、IMU 重力缩放和 PointCloud2 LIO 实测。
3. 在执行器断开状态完成全部路径、定位、TF 和 `/cmd_vel` 失效测试。
4. 审核履带符号、车辆限幅、下游看门狗、急停和首次测试速度。
5. 使用人工对齐路径完成低速直线、左弯、右弯、减速和终点停车。

### P1：GPS 与 LIO 恢复

当前只实现：定位故障锁存、零速、保存最后可信里程计和人工复位。以下能力尚未实现：

- GPS/全局位姿输入与质量门限；
- LIO supervisor 和重启命令；
- 利用 GPS 与最后可信里程计计算新的 `map→odom`；
- 连续多帧稳定后自动恢复行驶。

若 LIO 重启改变里程计原点，禁止只调用 `/localization/reset_odom_fault` 就继续行驶。必须保持零速，重新核对或更新 `map→odom`，确认坐标连续后再复位。

## 6. 外部依赖

雷达驱动和 LIO 为只读依赖，本仓库不修改、不复制它们：

| 依赖 | 本机参考源码 | 固定提交 |
|---|---|---|
| Livox ROS 2 驱动 | `/home/l1u/labs/livox_ros2_driver` | `e8fdb55d55a15412db25ab5bf9f3b7b875b5b9a6` |
| LIO Livox ROS 2 | `/home/l1u/labs/LIO_Livox_ROS2` | `fbfe521364f6f7daad61792ea761547bcfe4d956` |

RDK 部署版本记录保存在 `dependencies/rdk-deployment-lock.yaml`。设备不可用时不得把本机或 CI 结果描述成 RDK/实车验证通过。
